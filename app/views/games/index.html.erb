<h1>All Games</h1>

<ul>
  <% @games.each do |game| %>
  <li><%= game.street %>: <%= game.game_day.strftime('%b %eth, %Y') %> <%= game.game_time.in_time_zone('EST').strftime('%l:%M %p') %>
    <% if !game.opponent.empty? %>
      <%= "vs. #{game.opponent}" %></li>
    <% end %>
  <% end %>
</ul>
