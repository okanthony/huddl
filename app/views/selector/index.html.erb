<h1>Create Your Team</h1>

<%= form_for @team, url: teams_path do |f| %>
  <div class= "field">
    <%= f.label :name, "Team Name" %>
    <%= f.text_field :name %>
  </div>

  <div class= "field">
    <%= f.label :sport %>
    <%= f.select :sport, options_for_select(@sports) %>
  </div>

  <div class= "actions">
    <%= f.submit "Create Team" %>
  </div>
<% end %>
